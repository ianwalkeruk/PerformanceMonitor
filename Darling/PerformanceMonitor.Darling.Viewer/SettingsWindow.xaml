<Window x:Class="PerformanceMonitor.Darling.Viewer.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="750" Width="750"
        MinHeight="500" MinWidth="600"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Icon="EDD.ico"
        Background="{DynamicResource BackgroundBrush}">
    <!-- Faithful port of Performance Monitor Lite's SettingsWindow (Lite/Windows/SettingsWindow.xaml),
         section-for-section. Dark-only like the rest of the viewer: it does NOT merge ViewerDarkTheme.xaml
         (the two small write-back dialogs do) — it relies on the app-merged Themes/DarkTheme.xaml (App.xaml)
         for its implicit control styles + brush keys, exactly as the copied per-server / aggregate tab XAML
         does, so {DynamicResource BackgroundLightBrush} / {StaticResource AccentButton} / the PasswordBox +
         ComboBox + CheckBox chrome all resolve and render as they do in Lite.

         Persistence adaptation (matching the port): the alert/notification/SMTP/webhook/MCP sections drive
         Lite's in-process engine; in Darling those are the SERVICE's domain (darling.json). The viewer
         persists them faithfully in ViewerAppSettings so the window is complete and its state survives a
         restart — the service honoring a change is a separate wiring concern. The controls that act inside
         the viewer today work immediately: Manage Mute Rules (writes Postgres), Send Test Email / Send Test
         Notification (shared connection-independent renderers), and the folded-in viewer preferences
         (default time range + auto-refresh, formerly #1401). Two Lite-only mechanisms are adapted sensibly:
         Pause Collection can't reach the remote service (shown disabled/managed), and the per-server
         Collector Schedule editor has no store model in Darling (shown as an informational panel). -->
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="Hyperlink">
                <Setter Property="Foreground" Value="#6CB6FF"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <!-- #2063: two fixed rows — the settings scroll, the Save/Close bar stays pinned below the
         scroll (the Collector Schedules dialog's always-visible-actions shape), so a change made at
         the top of a long settings list saves without a scroll to the bottom. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="16,16,16,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="Configure the viewer and the Darling monitoring service"
                       FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Collection Control -->
        <Border Grid.Row="1" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Data Collection" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                        <TextBlock x:Name="CollectionStatusText" Text="Status: Managed by the Darling service" FontSize="12"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    <!-- The mnemonic sits on "Co_llection" rather than the verb so Alt+L works in both
                         states: the Click handler swaps this Content to "Resume Co_llection", and a key
                         that moved with the state would be a worse affordance than no key. -->
                    <Button Grid.Column="1" x:Name="PauseResumeButton" Content="Pause Co_llection"
                            Click="PauseResumeButton_Click" VerticalAlignment="Center"/>
                </Grid>
                <CheckBox x:Name="CapturePlansCheckBox" Content="Capture execution plans (all servers)" Margin="0,12,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"/>
                <CheckBox x:Name="QueryStoreBackfillCheckBox" Content="Query Store history backfill (all servers)" Margin="0,12,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"/>
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="70"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Query Store text budget (MB):" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="QueryStoreTextBudgetMbTextBox" Grid.Column="1" Margin="8,0,16,0"/>
                    <TextBlock Grid.Column="2" Text="Concurrent server sweeps:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="MaxConcurrentSweepsTextBox" Grid.Column="3" Margin="8,0,0,0"/>
                </Grid>
                <TextBlock Text="Collector memory bounds. The text budget caps how much query and plan text one database ships per collection pass (4-256, default 64); a smaller budget means shorter statements on the monitored server and slower history catch-up, never lost data — lower it when the fleet is a network hop away. Sweeps is how many servers collect at once (1-16, default 4); raise it when a large fleet queues behind collection on a host with headroom. Peak memory is roughly the two multiplied."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           TextWrapping="Wrap" Margin="0,4,0,0"/>
                <TextBlock Text="Fill Query Store history gaps (outages, newly added or freshly restored databases) in bounded background slices. Disable to stop a heavy catch-up — for example after restoring a large database onto a monitored server — live collection is unaffected; re-enabling resumes where it left off."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           TextWrapping="Wrap" Margin="24,2,0,0"/>
                <TextBlock Text="Store query and Query Store execution-plan XML across the fleet. Disable to shave storage on a very large fleet; a per-server override lives on each server's dialog."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="0,4,0,0" TextWrapping="Wrap"/>
                <TextBlock x:Name="SettingsReadOnlyBanner" FontSize="12" Foreground="{DynamicResource WarningTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"
                           Visibility="Collapsed"
                           Text="This viewer is connected with a read-only role. Monitoring settings below are shown for reference but can't be changed from here."/>
            </StackPanel>
        </Border>

        <!-- MCP Server Settings -->
        <Border Grid.Row="2" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="MCP Server (LLM Tool Access)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Allows LLM clients (Claude Code, etc.) to query monitoring data via the Model Context Protocol. The MCP server is hosted by the Darling service."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <CheckBox x:Name="McpEnabledCheckBox" Content="Enable MCP server" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="Port:" VerticalAlignment="Center" Margin="20,0,6,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="McpPortTextBox" Width="70" Text="5152" VerticalAlignment="Center"/>
                    <!-- Two "Auto" buttons exist on this page (MCP port here, web port below). The label
                         stays as-is because each sits beside its own Port box under its own section
                         heading; only the access keys differ, Alt+A here and Alt+O there. -->
                    <Button Content="_Auto" ToolTip="Find an available port"
                            Margin="6,0,0,0" Padding="8,2"
                            VerticalAlignment="Center"
                            Click="AutoPortButton_Click"/>
                </StackPanel>
                <TextBlock x:Name="McpStatusText" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,6,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Button x:Name="CopyMcpCommandButton" Content="Cop_y Setup Command" Click="CopyMcpCommandButton_Click"/>
                    <TextBlock Text="Works with Claude Code. Other LLMs may apply, but why would you?"
                               FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Web Dashboard Settings -->
        <Border Grid.Row="3" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Web Dashboard (Browser Access)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Serves a read-only monitoring dashboard over HTTP so a browser can view the fleet. Loopback-only by default; configure LAN exposure in darling.json. The dashboard is hosted by the Darling service."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <CheckBox x:Name="WebEnabledCheckBox" Content="Enable web dashboard" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="Port:" VerticalAlignment="Center" Margin="20,0,6,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="WebPortTextBox" Width="70" Text="5153" VerticalAlignment="Center"/>
                    <!-- Alt+O, because the MCP section's identical "Auto" button already claims Alt+A. -->
                    <Button Content="Aut_o" ToolTip="Find an available port"
                            Margin="6,0,0,0" Padding="8,2"
                            VerticalAlignment="Center"
                            Click="WebAutoPortButton_Click"/>
                </StackPanel>
                <TextBlock x:Name="WebStatusText" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,6,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Button x:Name="CopyWebUrlButton" Content="Copy _URL" Click="CopyWebUrlButton_Click"/>
                    <TextBlock Text="Open this URL in a browser on this machine (or across the LAN when exposed)."
                               FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Viewer Defaults -->
        <Border Grid.Row="4" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Viewer Defaults" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Default time range:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="DefaultTimeRangeCombo" Width="140" VerticalAlignment="Center"
                              ToolTip="Time window each newly-opened server tab starts on">
                        <ComboBoxItem Content="Last 1 hour"/>
                        <ComboBoxItem Content="Last 4 hours"/>
                        <ComboBoxItem Content="Last 12 hours"/>
                        <ComboBoxItem Content="Last 24 hours"/>
                        <ComboBoxItem Content="Last 7 days"/>
                    </ComboBox>
                </StackPanel>
                <!-- Folded in from #1401: the auto-refresh defaults for newly-opened server tabs. -->
                <CheckBox x:Name="AutoRefreshCheckBox" Content="Auto-refresh newly-opened server tabs"
                          Margin="0,10,0,0" Foreground="{DynamicResource ForegroundBrush}"
                          Checked="AutoRefreshCheckBox_Toggled" Unchecked="AutoRefreshCheckBox_Toggled"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Auto-refresh interval:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="AutoRefreshIntervalCombo" Width="140" VerticalAlignment="Center"
                              ToolTip="How often an auto-refreshing server tab reloads">
                        <ComboBoxItem Content="30 seconds"/>
                        <ComboBoxItem Content="1 minute"/>
                        <ComboBoxItem Content="5 minutes"/>
                    </ComboBox>
                </StackPanel>
                <!-- The shell's fleet cadence: one knob over what were two hardcoded timers (60s aggregate / 30s
                     Overview). Raise it to cut store-query load at fleet scale. -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Fleet refresh interval:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="NocRefreshIntervalCombo" Width="140" VerticalAlignment="Center"
                              ToolTip="How often the Overview and aggregate tabs refresh from the store. Raise it to cut store load when monitoring many servers.">
                        <ComboBoxItem Content="10 seconds" Tag="10"/>
                        <ComboBoxItem Content="30 seconds" Tag="30"/>
                        <ComboBoxItem Content="1 minute" Tag="60"/>
                        <ComboBoxItem Content="2 minutes" Tag="120"/>
                        <ComboBoxItem Content="5 minutes" Tag="300"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Connection timeout:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <TextBox x:Name="ConnectionTimeoutBox" Width="50" Text="5" VerticalAlignment="Center"/>
                    <TextBlock Text="seconds (5–60, increase for VPN/remote connections)"
                               FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                               VerticalAlignment="Center" Margin="6,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="CSV separator:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="CsvSeparatorCombo" Width="140" VerticalAlignment="Center">
                        <ComboBoxItem Content="Comma (,)" Tag=","/>
                        <ComboBoxItem Content="Semicolon (;)" Tag=";"/>
                        <ComboBoxItem Content="Tab" Tag="&#x9;"/>
                    </ComboBox>
                    <TextBlock Text="auto-detected from locale"
                               FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                               VerticalAlignment="Center" Margin="6,0,0,0" x:Name="CsvSeparatorHint"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Show timestamps in:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="TimeDisplayModeCombo" Width="140" VerticalAlignment="Center">
                        <ComboBoxItem Content="Server Time" Tag="ServerTime"/>
                        <ComboBoxItem Content="Local Time" Tag="LocalTime"/>
                        <ComboBoxItem Content="UTC" Tag="UTC"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Color theme:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="ColorThemeCombo" Width="140" VerticalAlignment="Center"
                              SelectionChanged="ColorThemeCombo_SelectionChanged">
                        <ComboBoxItem Content="Dark" Tag="Dark"/>
                        <ComboBoxItem Content="Light" Tag="Light"/>
                        <ComboBoxItem Content="Cool Breeze" Tag="CoolBreeze"/>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Notifications -->
        <Border Grid.Row="5" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Notifications" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center"/>
                    <Button x:Name="RestoreAlertDefaultsButton" Content="_Restore Defaults" Margin="12,0,0,0"
                            FontSize="11" VerticalAlignment="Center" Click="RestoreAlertDefaultsButton_Click"/>
                </StackPanel>
                <CheckBox x:Name="MinimizeToTrayCheckBox" Content="Minimize to system tray" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"/>
                <CheckBox x:Name="AlertsEnabledCheckBox" Content="Enable notifications" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="AlertsEnabledCheckBox_Changed" Unchecked="AlertsEnabledCheckBox_Changed"/>
                <CheckBox x:Name="NotifyConnectionCheckBox" Content="Connection lost / restored" Margin="20,6,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"/>
                <CheckBox x:Name="NotifyConnectionDownAtStartupCheckBox" Content="Alert if a server is already down when the service starts" Margin="36,4,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"
                          ToolTip="Off = the classic behavior: the first connect attempt is a silent baseline, even when it fails."/>
                <StackPanel Orientation="Horizontal" Margin="36,4,0,0"
                            ToolTip="0 = off (one alert per outage). Re-alerts use the same 'Server Unreachable' metric name, so webhook automation keyed on it re-triggers.">
                    <TextBlock Text="Re-alert while still down every" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="ConnectionRefireMinutesBox" Width="45" Text="0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="min (0 = off)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <CheckBox x:Name="NotifyAgHealthCheckBox" Content="Availability Group health" Margin="20,6,0,0"
                          Foreground="{DynamicResource ForegroundBrush}"
                          ToolTip="Failover, replica disconnected / reconnected, sync fell behind, and data movement suspended. Servers with no Availability Groups are unaffected."/>
                <StackPanel Orientation="Horizontal" Margin="36,4,0,0"
                            ToolTip="0 = off. How far a secondary may fall behind the primary before alerting.">
                    <TextBlock Text="Alert when a secondary is behind by" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AgLagAlertSecondsBox" Width="55" Text="300" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="sec (0 = off)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="36,4,0,0"
                            ToolTip="0 = off. A healthy redo queue size is workload-specific, so this ships off — set it once you know your own baseline.">
                    <TextBlock Text="…or its redo queue reaches" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AgRedoQueueAlertKbBox" Width="95" Text="0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="KB (0 = off)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="36,4,0,0"
                            ToolTip="0 = off (one alert per outage). Re-alerts use the same 'AG Replica Disconnected' metric name, so webhook automation keyed on it re-triggers.">
                    <TextBlock Text="Re-alert while a replica is still disconnected every" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AgDisconnectRefireMinutesBox" Width="45" Text="0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="min (0 = off)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertCpuCheckBox" Content="CPU above" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertCpuThresholdBox" Width="45" Text="80" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="measured as" VerticalAlignment="Center" Margin="8,0,4,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <ComboBox x:Name="AlertCpuModeBox" Width="220" VerticalAlignment="Center">
                        <ComboBoxItem Content="Total non-idle CPU (SQL + other processes)" Tag="Total"/>
                        <ComboBoxItem Content="SQL Server only (scheduler %)" Tag="SqlOnly"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertBlockingCheckBox" Content="Blocking sessions &#x2265;" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertBlockingThresholdBox" Width="45" Text="1" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Total blocked wait &#x2265;" VerticalAlignment="Center" Margin="24,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertBlockingWaitSecondsBox" Width="45" Text="0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="seconds (0 = off)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="PostgreSQL blocking roots &#x2265;" VerticalAlignment="Center" Margin="24,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPgBlockingThresholdBox" Width="45" Text="1" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="(sampled root blockers; the checkbox above covers both engines)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertDeadlockCheckBox" Content="Deadlocks &#x2265;" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertDeadlockThresholdBox" Width="45" Text="1" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="PostgreSQL deadlocks &#x2265;" VerticalAlignment="Center" Margin="24,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPgDeadlockThresholdBox" Width="45" Text="1" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="(the checkbox above covers both engines)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertPoisonWaitCheckBox" Content="Poison waits (THREADPOOL, etc.) avg wait &#x2265;" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPoisonWaitThresholdBox" Width="45" Text="500" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertLongRunningQueryCheckBox" Content="Long-running queries over" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertLongRunningQueryThresholdBox" Width="45" Text="30" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes," VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="return up to" VerticalAlignment="Center" Margin="6,0,6,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertLongRunningQueryMaxResultsBox" Width="45" Text="5" VerticalAlignment="Center"/>
                    <TextBlock Text="results" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- Long Running Query Filters -->
                <TextBlock Text="Long Running Query Filters" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,10,0,4"/>
                <!-- Underscores in these wait/proc names are doubled: WPF reads a single _ as an
                     access-key marker, which both swallows the character and claims an Alt key. -->
                <CheckBox x:Name="LrqExcludeSpServerDiagnosticsCheckBox"
                          Content="Exclude SP__SERVER__DIAGNOSTICS"
                          Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,4"/>
                <CheckBox x:Name="LrqExcludeWaitForCheckBox"
                          Content="Exclude WAITFOR / BROKER__RECEIVE__WAITFOR"
                          Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,4"/>
                <CheckBox x:Name="LrqExcludeBackupsCheckBox"
                          Content="Exclude backup waits (BACKUPTHREAD, BACKUPIO)"
                          Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,4"/>
                <CheckBox x:Name="LrqExcludeMiscWaitsCheckBox"
                          Content="Exclude miscellaneous waits (XE__LIVE__TARGET__TVF)"
                          Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,4"/>
                <CheckBox x:Name="LrqExcludeCdcCheckBox"
                          Content="Exclude CDC capture jobs (sp__MScdc__capture__job, sp__cdc__scan)"
                          Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertTempDbSpaceCheckBox" Content="tempdb space usage over" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertTempDbSpaceThresholdBox" Width="45" Text="80" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertLowDiskCheckBox" Content="Volume free space under" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertLowDiskThresholdPercentBox" Width="45" Text="10" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% or" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertLowDiskThresholdGbBox" Width="45" Text="5" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="GB free (Azure SQL DB excluded)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- #2107: the CRITICAL severity tier's floors (#1136) — previously compile-time. -->
                <StackPanel Orientation="Horizontal" Margin="40,4,0,0">
                    <TextBlock Text="grade CRITICAL at" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertDiskCriticalPercentBox" Width="45" Text="3" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% or" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertDiskCriticalGbBox" Width="45" Text="2" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="GB free" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- #2107: the monitor's own self-alerts — store volume + collection health. -->
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Monitor self-alerts: store volume under" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertSelfDiskWarnPercentBox" Width="45" Text="10" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% free and under" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertSelfDiskWarnGbBox" Width="45" Text="50" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="GB (0 GB = percent alone); collection quiet" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertCollectionStaleMinutesBox" Width="45" Text="30" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="min or" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertCollectionFailureThresholdBox" Width="45" Text="10" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="straight failures" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- #2136: the store's own background jobs — warn when a run reaches this percent of its cadence. -->
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Store background job runs longer than" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertStoreJobCadenceWarnPercentBox" Width="45" Text="25" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% of its schedule interval (critical at 100%)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- #3297: the Retention Held tiers, which were compile-time constants — so this alert named
                     "Retention Held" against "Monitor Store" had nothing here matching either word (#3296). -->
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Retention held past" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertRetentionHoldWarnRatioBox" Width="45" Text="2.0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="x its horizon; critical at" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertRetentionHoldCriticalRatioBox" Width="45" Text="4.0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="x (2.0 or above; healthy chunk granularity reaches 1.4x)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertPvsCheckBox" Content="Version store (PVS) over" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPvsThresholdPercentBox" Width="45" Text="40" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% of database and at least" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPvsFloorGbBox" Width="45" Text="1" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="GB (ADR databases only)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertFileGrowthCheckBox" Content="Database file grew over" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertFileGrowthRiseMbBox" Width="60" Text="10240" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="MB within" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertFileGrowthLookbackMinutesBox" Width="45" Text="60" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="min, or is over" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertFileGrowthVolumePercentBox" Width="45" Text="60" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="% of its volume (0 disables either gate)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertLongRunningJobCheckBox" Content="Agent jobs running over" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertLongRunningJobMultiplierBox" Width="35" Text="3" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="x historical average" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertFailedJobCheckBox" Content="Agent job failed in the last" VerticalAlignment="Center"
                              Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertFailedJobLookbackBox" Width="45" Text="60" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <CheckBox x:Name="AlertDatabaseStateCheckBox" Content="Database state changed from its expected (baseline) state"
                              VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}"/>
                    <Button x:Name="ConfigureDatabaseStatesButton" Content="Configure…" Margin="8,0,0,0" Padding="8,2"
                            VerticalAlignment="Center" Click="ConfigureDatabaseStatesButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,10,0,0">
                    <TextBlock Text="Tray notification cooldown:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertCooldownBox" Width="40" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Email alert cooldown:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="EmailCooldownBox" Width="40" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <!-- deadlock/blocking notification delivery mode + per-event cap -->
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Deadlock/blocking delivery:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <ComboBox x:Name="AlertDeliveryModeBox" Width="260" VerticalAlignment="Center">
                        <ComboBoxItem Content="Summary — one card per cycle" Tag="Summary"/>
                        <ComboBoxItem Content="Per-event — one notification per incident" Tag="PerEvent"/>
                    </ComboBox>
                    <TextBlock Text="max" VerticalAlignment="Center" Margin="8,0,4,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AlertPerEventMaxBox" Width="40" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="/cycle" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <TextBlock x:Name="AlertPreviewText" FontSize="11" FontStyle="Italic"
                           Foreground="{DynamicResource ForegroundMutedBrush}" TextWrapping="Wrap" Margin="20,10,0,0"/>
                <!-- #3368: Server Health Bands. Its own section, and NOT an alert section — these two tiers
                     decide what colour a server's Overview card and the fleet roll-up read, not whether
                     anything is delivered. They are deliberately absent from SetAlertControlsEnabled for
                     that reason: a deployment with alerting switched off still renders bands. -->
                <TextBlock Text="Server Health Bands" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,14,0,4"/>
                <TextBlock Text="Card and fleet-roll-up colours, not alert delivery — these stay in effect with alerting switched off."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,0,0,4" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                    <TextBlock Text="Deadlocks warn at" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="BandDeadlockWarnPerHourBox" Width="45" Text="5.0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="per hour; critical at" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="BandDeadlockCriticalPerHourBox" Width="45" Text="20.0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="per hour (1.0 or above; a typical busy server-hour measures 1-2)" VerticalAlignment="Center" Margin="2,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <TextBlock Text="Normalised over whatever window a surface asks for, so one pair means the same condition on a 1-hour and a 24-hour read."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,2,0,0" TextWrapping="Wrap"/>

                <!-- Global Alert Filters -->
                <TextBlock Text="Global Alert Filters" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,14,0,4"/>
                <TextBlock Text="Exclude databases from alerts (comma-separated):"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,0,0,4" FontSize="12"/>
                <TextBox x:Name="AlertExcludedDatabasesBox" Margin="20,0,0,4"/>
                <TextBlock Text="e.g. DBAtools, msdb, tempdb — applies to blocking, deadlock, and long-running query alerts"
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,0,0,0" TextWrapping="Wrap"/>

                <!-- Mute Rules -->
                <TextBlock Text="Alert Muting" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,14,0,4"/>
                <StackPanel Orientation="Horizontal" Margin="20,0,0,4">
                    <TextBlock Text="Default expiration for new mute rules:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <ComboBox x:Name="MuteRuleDefaultExpirationCombo" Width="130" VerticalAlignment="Center">
                        <ComboBoxItem Content="1 hour"/>
                        <ComboBoxItem Content="24 hours"/>
                        <ComboBoxItem Content="7 days"/>
                        <ComboBoxItem Content="Never"/>
                    </ComboBox>
                </StackPanel>
                <Button x:Name="ManageMuteRulesButton" Content="_Manage Mute Rules..."
                        Click="ManageMuteRulesButton_Click" HorizontalAlignment="Left"
                        Padding="12,4" Margin="20,4,0,4"/>
                <TextBlock Text="Create rules to suppress specific recurring alerts while still logging them."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,0,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="LogAlertDismissalsCheckBox" Content="Log alert dismiss and mute actions"
                          Margin="20,8,0,0" Foreground="{DynamicResource ForegroundBrush}"/>

                <!-- Automated Analysis -->
                <TextBlock Text="Automated Analysis" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,14,0,4"/>
                <TextBlock Text="Periodically run the triage engine and record findings in the Alerts tab. Notifications below are optional — analysis runs and stores findings even with notifications off."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,0,0,4" TextWrapping="Wrap"/>
                <CheckBox x:Name="AnalysisEnabledCheckBox" Content="Enable automated analysis"
                          Margin="20,4,0,0" Foreground="{DynamicResource ForegroundBrush}"/>
                <CheckBox x:Name="AnalysisNotificationsCheckBox" Content="Send scheduled analysis notifications"
                          Margin="20,4,0,0" Foreground="{DynamicResource ForegroundBrush}"/>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Run analysis every" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AnalysisIntervalBox" Width="45" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Notify on finding severity at or above" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AnalysisNotifySeverityBox" Width="45" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="(0.0 - 2.0)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
                <!-- #2107: was a hardcoded 360 while Lite always honored a configured value. -->
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Re-notify an unchanged finding after" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="AnalysisNotifyCooldownBox" Width="55" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes (30 - 10080)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
                <TextBlock Text="Requires SMTP or a webhook configured below for delivery; findings are recorded in the Alerts tab either way."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,4,0,0" TextWrapping="Wrap"/>

                <!-- #3466 (V124): Fleet Sweep Reports. Deliberately NOT gated by the master alerts toggle:
                     sweeps keep running under a fleet-wide mute and carry the would-have-paged ledger,
                     which is the muted-mode contract's whole point. -->
                <TextBlock Text="Fleet Sweep Reports" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="20,14,0,4"/>
                <TextBlock Text="Periodically sweep the whole fleet into a stateful report: per-server health verdicts, changes since the previous sweep, and watch items. Runs even when alerts are disabled — a muted fleet still gets a readable ledger of what would have paged."
                           FontSize="11" FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"
                           Margin="20,0,0,4" TextWrapping="Wrap"/>
                <CheckBox x:Name="FleetSweepEnabledCheckBox" Content="Enable fleet sweep reports"
                          Margin="20,4,0,0" Foreground="{DynamicResource ForegroundBrush}"/>
                <StackPanel Orientation="Horizontal" Margin="20,6,0,0">
                    <TextBlock Text="Sweep the fleet every" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBox x:Name="FleetSweepIntervalBox" Width="45" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="minutes (15 - 1440)" VerticalAlignment="Center" Margin="4,0,0,0"
                               Foreground="{DynamicResource ForegroundMutedBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Email Alerts (Optional) -->
        <Border Grid.Row="6" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Email Alerts (Optional)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Send email notifications alongside system tray alerts. Requires an SMTP server."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="SmtpEnabledCheckBox" Content="Enable email alerts" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="SmtpEnabledCheckBox_Changed" Unchecked="SmtpEnabledCheckBox_Changed"/>
                <Grid Margin="20,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="SMTP Server:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="SmtpServerBox" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Port:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBox x:Name="SmtpPortBox" Width="70" Text="587"/>
                        <CheckBox x:Name="SmtpSslCheckBox" Content="Use SSL/TLS" VerticalAlignment="Center" Margin="16,0,0,0"
                                  Foreground="{DynamicResource ForegroundBrush}" IsChecked="True"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Username:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="SmtpUsernameBox" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Password:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="SmtpPasswordBox" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="From Address:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="4" Grid.Column="1" x:Name="SmtpFromBox" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Recipients:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="5" Grid.Column="1" x:Name="SmtpRecipientsBox" Margin="0,0,0,4"
                             ToolTip="Comma-separated email addresses"/>

                    <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <Button x:Name="TestEmailButton" Content="Send Test _Email" Click="TestEmailButton_Click"/>
                        <Button x:Name="ValidateSmtpButton" Content="_Validate Settings" Margin="8,0,0,0"
                                Click="ValidateSmtpButton_Click"/>
                        <TextBlock x:Name="SmtpStatusText" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Webhooks (Teams / Slack) -->
        <Border Grid.Row="7" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <!-- Microsoft Teams -->
                <TextBlock Text="Microsoft Teams Notifications" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Send alert notifications to a Teams channel via incoming webhook. Color-coded accent bars indicate alert severity."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="TeamsWebhookEnabledCheckBox" Content="Enable Teams notifications" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="TeamsWebhookEnabledCheckBox_Changed" Unchecked="TeamsWebhookEnabledCheckBox_Changed"/>
                <Grid Margin="20,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Webhook URL:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="TeamsWebhookUrlBox" Margin="0,0,0,4"
                             ToolTip="Paste your Teams Incoming Webhook or Power Automate Workflows URL here"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Proxy Address:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="TeamsProxyAddressBox" Margin="0,0,0,4"
                             ToolTip="Optional HTTP proxy (e.g. http://proxy.corp:8080). Leave blank for direct connection."/>

                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <!-- Named per channel: the Teams, Slack and generic-webhook buttons all read
                             "Send Test Notification" before, which are three identical labels visible at
                             once on one scrolling page and so could not carry distinct access keys. -->
                        <Button x:Name="TestTeamsButton" Content="Send Test to _Teams" Click="TestTeamsButton_Click"/>
                        <TextBlock x:Name="TeamsStatusText" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
                <TextBlock Margin="20,8,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Text="Setup: Create an Incoming Webhook connector or a Power Automate Workflows webhook for your Teams channel. "/>
                    <Hyperlink NavigateUri="https://learn.microsoft.com/en-us/microsoftteams/platform/webhooks-and-connectors/how-to/add-incoming-webhook"
                               RequestNavigate="Hyperlink_RequestNavigate">
                        Teams Webhooks Documentation
                    </Hyperlink>
                </TextBlock>

                <!-- Slack -->
                <TextBlock Text="Slack Notifications" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,20,0,0"/>
                <TextBlock Text="Send alert notifications to a Slack channel via incoming webhook. Color-coded sidebar indicates alert severity."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="SlackWebhookEnabledCheckBox" Content="Enable Slack notifications" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="SlackWebhookEnabledCheckBox_Changed" Unchecked="SlackWebhookEnabledCheckBox_Changed"/>
                <Grid Margin="20,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Webhook URL:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="SlackWebhookUrlBox" Margin="0,0,0,4"
                             ToolTip="Paste your Slack Incoming Webhook URL here"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Proxy Address:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="SlackProxyAddressBox" Margin="0,0,0,4"
                             ToolTip="Optional HTTP proxy (e.g. http://proxy.corp:8080). Leave blank for direct connection."/>

                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <Button x:Name="TestSlackButton" Content="Send Test to Slac_k" Click="TestSlackButton_Click"/>
                        <TextBlock x:Name="SlackStatusText" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
                <TextBlock Margin="20,8,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Text="Setup: Create an Incoming Webhook app in your Slack workspace settings. "/>
                    <Hyperlink NavigateUri="https://api.slack.com/messaging/webhooks"
                               RequestNavigate="Hyperlink_RequestNavigate">
                        Slack Webhooks Documentation
                    </Hyperlink>
                </TextBlock>

                <!-- PagerDuty -->
                <TextBlock Text="PagerDuty Notifications" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,20,0,0"/>
                <TextBlock Text="Send alert notifications to PagerDuty via the Events API v2. Alerts trigger a PagerDuty incident; repeated alerts for the same condition correlate into the same incident."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="PagerDutyWebhookEnabledCheckBox" Content="Enable PagerDuty notifications" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="PagerDutyWebhookEnabledCheckBox_Changed" Unchecked="PagerDutyWebhookEnabledCheckBox_Changed"/>
                <Grid Margin="20,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Routing Key:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="PagerDutyRoutingKeyBox" Margin="0,0,0,4"
                             ToolTip="The 32-character Integration Key from a PagerDuty Events API v2 integration"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="" VerticalAlignment="Center" Margin="0,0,8,4"/>
                    <CheckBox Grid.Row="1" Grid.Column="1" x:Name="PagerDutyEuRegionCheckBox" Content="Use EU data center (events.eu.pagerduty.com)"
                              Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="" VerticalAlignment="Center" Margin="0,0,8,4"/>
                    <CheckBox Grid.Row="2" Grid.Column="1" x:Name="PagerDutyAutoResolveCheckBox" Content="Auto-resolve the incident when the server recovers"
                              Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"
                              ToolTip="Sends a PagerDuty 'resolve' event when a server comes back, closing the incident opened by 'Server Unreachable'. Off by default: the recovery is sent as an info-level alert and the incident stays open."/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Proxy Address:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="PagerDutyProxyAddressBox" Margin="0,0,0,4"
                             ToolTip="Optional. HTTP proxy for reaching PagerDuty (e.g. http://proxy.corp.local:8080). Leave blank to connect directly."/>

                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <Button x:Name="TestPagerDutyButton" Content="Send Test Notification" Click="TestPagerDutyButton_Click"/>
                        <TextBlock x:Name="PagerDutyStatusText" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
                <TextBlock Margin="20,8,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Text="Setup: Create a service in PagerDuty, add an Events API v2 integration, and copy its Integration Key. "/>
                    <Hyperlink NavigateUri="https://support.pagerduty.com/docs/services-and-integrations"
                               RequestNavigate="Hyperlink_RequestNavigate">
                        PagerDuty Integrations Documentation
                    </Hyperlink>
                </TextBlock>

                <!-- Generic webhook -->
                <TextBlock Text="Generic Webhook (HTTP POST)" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,20,0,0"/>
                <TextBlock Text="POST alerts as JSON to any endpoint — PagerDuty, Opsgenie, n8n, or a GitHub repository_dispatch that re-runs a workflow. Use this to trigger automation when an alert fires (for example, re-adding your IP to an Azure SQL firewall rule after a connection is lost)."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <CheckBox x:Name="GenericWebhookEnabledCheckBox" Content="Enable generic webhook" Margin="0,8,0,0"
                          Foreground="{DynamicResource ForegroundBrush}" Checked="GenericWebhookEnabledCheckBox_Changed" Unchecked="GenericWebhookEnabledCheckBox_Changed"/>
                <Grid Margin="20,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="URL:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="GenericWebhookUrlBox" Margin="0,0,0,4"
                             ToolTip="The endpoint to POST to, e.g. https://api.github.com/repos/OWNER/REPO/dispatches"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Headers (JSON):" VerticalAlignment="Top"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,4,8,4"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="GenericWebhookHeadersBox" Margin="0,0,0,4"
                             AcceptsReturn="True" TextWrapping="Wrap" MinHeight="44" VerticalScrollBarVisibility="Auto"
                             ToolTip="A JSON object of request headers. Held as a secret in the control plane — it carries your token."/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Body (JSON):" VerticalAlignment="Top"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,4,8,4"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="GenericWebhookBodyBox" Margin="0,0,0,4"
                             AcceptsReturn="True" TextWrapping="Wrap" MinHeight="80" VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             ToolTip="The JSON body to POST. Leave blank to use the built-in default. Placeholders are substituted and JSON-escaped."/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Proxy Address:" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="GenericWebhookProxyAddressBox" Margin="0,0,0,4"
                             ToolTip="Optional HTTP proxy (e.g. http://proxy.corp:8080). Leave blank for direct connection."/>

                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <Button x:Name="TestGenericButton" Content="Send Test to _Webhook" Click="TestGenericButton_Click"/>
                        <TextBlock x:Name="GenericStatusText" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
                <TextBlock Margin="20,8,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Text="Placeholders (each is JSON-escaped when substituted): "/>
                    <Run FontFamily="Consolas" Text="{}{{metric}} {{server}} {{value}} {{threshold}} {{severity}} {{context}} {{detail}} {{timestamp}}"/>
                </TextBlock>
                <TextBlock Margin="20,4,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Text="Automation placeholders — the first two substitute RAW JSON values (use them unquoted, e.g. &quot;context&quot;: {{context_json}}): "/>
                    <Run FontFamily="Consolas" Text="{}{{context_json}} {{incidents_json}} {{dedup_key}} {{resource_name}} {{database}} {{triage_url}}"/>
                </TextBlock>
                <TextBlock Margin="20,6,0,0" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run FontWeight="SemiBold" Text="Example — re-run a GitHub Actions workflow when an alert fires:"/>
                    <LineBreak/>
                    <Run FontFamily="Consolas" Text="URL: https://api.github.com/repos/OWNER/REPO/dispatches"/>
                    <LineBreak/>
                    <Run FontFamily="Consolas" Text="{}Headers: {&quot;Authorization&quot;: &quot;Bearer ghp_YOUR_TOKEN&quot;, &quot;Accept&quot;: &quot;application/vnd.github+json&quot;}"/>
                    <LineBreak/>
                    <Run FontFamily="Consolas" Text="{}Body: {&quot;event_type&quot;: &quot;sql-monitor-alert&quot;, &quot;client_payload&quot;: {&quot;metric&quot;: &quot;{{metric}}&quot;, &quot;server&quot;: &quot;{{server}}&quot;}}"/>
                    <LineBreak/>
                    <Run Text="The repository's workflow triggers on "/>
                    <Run FontFamily="Consolas" Text="repository_dispatch"/>
                    <Run Text=". The token needs Contents: write on that repository."/>
                </TextBlock>

                <!-- Severity Color Reference -->
                <TextBlock Text="Alert Severity Colors" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,16,0,4"/>
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,0,2">
                    <Run Foreground="#DC2626" FontWeight="Bold" Text="&#x25A0; Red" /> — Critical (Deadlocks, Poison Waits, Server Unreachable)
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,0,2">
                    <Run Foreground="#D97706" FontWeight="Bold" Text="&#x25A0; Orange" /> — Alert / Warning (Blocking, Long-Running Queries, tempdb, Jobs)
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,0,2">
                    <Run Foreground="#F59E0B" FontWeight="Bold" Text="&#x25A0; Yellow" /> — Warning (High CPU)
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,0,2">
                    <Run Foreground="#16A34A" FontWeight="Bold" Text="&#x25A0; Green" /> — Resolved (Server Restored)
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}">
                    <Run Foreground="{DynamicResource InfoBrush}" FontWeight="Bold" Text="&#x25A0; Blue" /> — Info
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Collection Schedule (control-plane editor) -->
        <Border Grid.Row="8" Background="{DynamicResource BackgroundLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Collection Schedule" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Tune how often the Darling service collects each metric and how long it keeps it — fleet-wide or per server. Blank overrides fall back to the built-in defaults; presets bulk-set frequencies."
                           FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                <Button x:Name="EditSchedulesButton" Content="E_dit Collector Schedules..." HorizontalAlignment="Left"
                        Padding="12,4" Margin="0,8,0,0" Click="EditSchedulesButton_Click"/>
            </StackPanel>
        </Border>

    </Grid>
    </ScrollViewer>

        <!-- Pinned action bar (#2063) — outside the ScrollViewer, always visible. -->
        <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,10,16,12">
                <Button Content="_Save" Click="SaveButton_Click" Style="{DynamicResource AccentButton}" Margin="0,0,8,0"/>
                <Button Content="_Close" Click="CloseButton_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
